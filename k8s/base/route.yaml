apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mcp-server-aap
  labels:
    app: mcp-server-aap
    app.kubernetes.io/name: mcp-server-aap
    app.kubernetes.io/component: server
spec:
  to:
    kind: Service
    name: mcp-server-aap
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None

