apiVersion: v1
kind: Secret
metadata:
  name: mcp-server-aap-secret
  labels:
    app: mcp-server-aap
    app.kubernetes.io/name: mcp-server-aap
    app.kubernetes.io/component: server
type: Opaque
stringData:
  # AAP Access Token - REPLACE WITH YOUR ACTUAL TOKEN
  # You can also create this secret manually or use sealed-secrets/external-secrets
  aap-token: "your-aap-token-here"

