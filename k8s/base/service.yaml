apiVersion: v1
kind: Service
metadata:
  name: mcp-server-aap
  labels:
    app: mcp-server-aap
    app.kubernetes.io/name: mcp-server-aap
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app: mcp-server-aap
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: None

